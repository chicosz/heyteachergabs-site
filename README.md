# Hey Teacher Gabs · site (preview)

- `index.html` — home v3 (sistema GoCore, paleta Gabs). VSL: preencha `data-src` em `#htg-vsl`.
- `nivelamento.html` — teste adaptativo. Supabase: `data-endpoint` / `data-apikey` em `#q`.
- `v2.html` — versão anterior, só pra comparar.
